[project]
name = "school-final-project"
version = "0.2.0"
description = "Monthly budgeting app with CLI and Tkinter GUI"
authors = [{ name = "Student" }]
readme = "README.md"
requires-python = ">=3.9"
